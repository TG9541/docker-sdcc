# Minimal test Dockerfile for GHCR pushing
FROM ubuntu:24.04

# Install minimal build tools
RUN apt-get update \
    && apt-get install -y build-essential \
    && apt-get autoclean

# Verify installation
RUN echo "Build tools installed successfully" && \
    gcc --version && \
    make --version
